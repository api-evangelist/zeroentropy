overlay: 1.0.0
info:
  title: API Evangelist enrichment overlay for ZeroEntropy API
  version: 1.0.0
extends: ../openapi/zeroentropy-openapi.json
x-generated: '2026-07-21'
x-method: generated
x-source: openapi/zeroentropy-openapi.json
actions:
- target: $.info
  description: Annotate the API with API Evangelist network provenance.
  update:
    x-api-evangelist:
      provider: zeroentropy
      network: https://apis.io/providers/zeroentropy/
      category: AI retrieval / reranking / embeddings
- target: $.info
  description: Record the documented authentication model (HTTP Bearer API key).
  update:
    x-authentication:
      style: bearer
      credential: api-key
      header: Authorization
      docs: https://docs.zeroentropy.dev/api-reference/
- target: $.info
  description: Record list-endpoint pagination convention (keyset on path).
  update:
    x-pagination:
      style: keyset
      params:
      - limit
      - path_gt
      applies_to:
      - /documents/get-document-info-list
